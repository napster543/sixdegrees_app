import { FiltrarJsonPipe } from './filtrar-json.pipe';

describe('FiltrarJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
